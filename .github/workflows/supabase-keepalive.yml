name: Supabase Keep-Alive

on:
  schedule:
    # Runs every Monday at 10:00 AM UTC
    - cron: '0 10 * * 1'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Query Supabase to keep project active
        run: |
          npm install @supabase/supabase-js --silent
          node << 'EOF'
          const { createClient } = require('@supabase/supabase-js');
          
          const projectUrl = 'https://qdyuftmeqaudncfgmrlz.supabase.co';
          const anonKey = process.env.SUPABASE_ANON_KEY;
          
          console.log('Connecting to Supabase...');
          console.log('Project URL:', projectUrl);
          
          const supabase = createClient(projectUrl, anonKey);
          
          (async () => {
            try {
              console.log('Querying guests_emails table...');
              const { data, error } = await supabase
                .from('guests_emails')
                .select('*')
                .limit(1);
              
              if (error) {
                console.error('❌ Query error:', error.message);
                process.exit(1);
              }
              
              console.log('✅ Query successful!');
              console.log('Rows returned:', data?.length || 0);
              process.exit(0);
            } catch (err) {
              console.error('❌ Exception:', err.message);
              process.exit(1);
            }
          })();
          EOF
        env:
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Log timestamp
        if: success()
        run: echo "✅ Keep-alive completed at $(date)"
